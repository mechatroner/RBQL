1	QueryColumnInfo(table_name=None, column_index=None, column_name=None, is_star=True)
2	'QueryColumnInfo', ['table_name', 'column_index', 'column_name', 'is_star']
2	root, ast.Attribute
1	table_name is None or table_name not in ['a', 'b']
1	get_field(slice_val_root, 'n') - 1
3	cur_sum + val, cur_sum_of_squares + val ** 2, cur_cnt + 1
1	'Invalid aggregate expression: non-constant values in output column {}. E.g. "{}" and "{}"'.format(self.output_index + 1, old_value, value)
1	isinstance(args[0], int) or isinstance(args[0], float)
3	NONE, join_map.max_record_len, [None] * join_map.max_record_len
3	embed_code(python_code, '__CODE__', PROCESS_SELECT_SIMPLE), '__CODE__', PROCESS_SELECT_COMMON
1	"\nRBQL doesn't support \"LIKE\" operator, use like() function instead e.g. ... WHERE like(a1, 'foo%bar') ... "
1	re.findall('[-a-zA-Z0-9_:;+=!.,()%^#@&* ]+', column_name)
1	dst_variables_map['{}["{}"]'.format(prefix, python_string_escape_column_name(column_name, '"'))]
1	'Unable to find column "{}" in {} {}'.format(column_name, {'a': 'input', 'b': 'join'}[prefix], column_names_source)
1	[STRICT_LEFT_JOIN, LEFT_OUTER_JOIN, LEFT_JOIN, INNER_JOIN, JOIN]
1	{JOIN: InnerJoiner, INNER_JOIN: InnerJoiner, LEFT_OUTER_JOIN: LeftJoiner, LEFT_JOIN: LeftJoiner, STRICT_LEFT_JOIN: StrictLeftJoiner}[rb_actions[JOIN]['join_subtype,foo'] + foo(a, b)]
2	'""', '"'
